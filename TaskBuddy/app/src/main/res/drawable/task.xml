<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape
            android:shape="rectangle">
            <solid android:color="@color/task" /> <!-- Background color of the shape -->
            <corners android:radius="25dp" /> <!-- Radius of the corners (adjust as needed) -->
        </shape>
    </item>
</layer-list>
